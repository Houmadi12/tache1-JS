/**
 * Type primitives
 * - String
 * - number
 * - bigint
 * - boolean
 * - null
 * - undefined 
 * - Symbol
 * 
 * Refersnces type
 * - objet
 * - (array)
 */

let maVariable;

console.log(maVariable); // Ici on aura comme variable undifined car la variable n'est pas assigné

maVariable = "Melvyn";

console.log(maVariable); //Ici on aura comme réponse "Malvyn"

//Les variable en JS sont dynamiques c'est à dire ça peut changer d'un type à un autre

maVariable = 144; //Ici on aura 144 en sortie 

//Déclarer un constante 
const myConstante = "Ambdou";

console.log(myConstante); //Ici le variable a comme valeur : Ambdou

// myConstante = 144 //Erreur myConstante est un variable constante 